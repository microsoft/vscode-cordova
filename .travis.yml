branches:
  only:
    - master

sudo: false

os:
  - osx
env:
  - NODE_VERSION="6.14.4"

before_install:
  # nvm is not yet supported on osx, manually install
  - git clone https://github.com/creationix/nvm.git /tmp/.nvm
  - source /tmp/.nvm/nvm.sh
  - nvm install $NODE_VERSION
  - nvm use --delete-prefix $NODE_VERSION

language: node_js

node_js:
  - "lts/*"

install:
  - npm install

before_script:
  - npm install -g gulp-cli
  - npm install -g cordova@5.4.0
  - export CODE_TESTS_WORKSPACE="${PWD}/test/testProject"
  - export CODE_VERSION="1.17.0"

script:
  - npm run vscode:prepublish
  - npm test